import sys

#assign variables
hostname = sys.argv[1]
username = sys.argv[2]

#if not USERNAME:
#    USERNAME = admin

print('The hostname submitted was: ' + hostname + ' and the username was: ' + username)
